# Optional: specify custom backend URL during development or standalone deployment
# If left empty, requests default to relative path (/api/...) and proxy through Nginx or Vite
VITE_API_URL=http://localhost:8080
